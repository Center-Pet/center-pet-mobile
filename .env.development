# =============================================================================
# Expo — desenvolvimento. NÃO commite. Ver .env.example para origens.
# =============================================================================

# API pública — Render ou localhost; fallback em src-mobile/config/api.js se vazio
EXPO_PUBLIC_API_URL=https://centerpet-api.onrender.com/api

# EAS Build / expo export — tokens em New Relic → Mobile → Settings (opcional em dev)
NEW_RELIC_IOS_APP_TOKEN=
NEW_RELIC_ANDROID_APP_TOKEN=
